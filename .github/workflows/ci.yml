name: CI

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: |-
    ${{ github.ref != format('refs/heads/{0}', github.event.repository.default_branch) }}

on:
  push:
    branches:
      - main
      - epic/*
      - "[0-9]+.[0-9]+.x"
  pull_request:
    branches:
      - main
      - epic/*
      - "[0-9]+.[0-9]+.x"
  workflow_dispatch:

jobs:
  doc:
    uses: ComPWA/actions/.github/workflows/ci-docs.yml@fbe7fd9b7edbd7bbf9f6881fe36e66cb4871b3dc
    permissions:
      pages: write
      id-token: write
    with:
      gh-pages: true
  style:
    secrets:
      token: ${{ secrets.PAT }}
    uses: ComPWA/actions/.github/workflows/style.yml@fbe7fd9b7edbd7bbf9f6881fe36e66cb4871b3dc
  test:
    uses: ComPWA/actions/.github/workflows/test.yml@fbe7fd9b7edbd7bbf9f6881fe36e66cb4871b3dc
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
    with:
      coverage-python-version: "3.13"
      macos-python-version: "3.10"
      multithreaded: false
